/**
 * Transport Item
 */
//School.Collection.Sale.before.insert(function (userId, doc) {
//    doc.saleItemId = itemId;
//});

//School.Collection.Sale.before.update(function (userId, doc, fieldNames, modifier, options) {
//    modifier.$set = modifier.$set || {};
//    var items = [];
//    _.each(modifier.$set.items, function (obj) {
//        if (!_.isNull(obj)) {
//            itemsDoc = obj;
//            items.push(itemsDoc);
//        }
//    });
//    modifier.$set.items = items;
//
//});
